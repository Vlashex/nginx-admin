import { describe, it } from "vitest";

describe("SSHExecutor abort behavior", () => {
  it.todo("aborts while connecting and rejects with RemoteExecutionError code ABORTED");

  it.todo("aborts active command stream and rejects with RemoteExecutionError code ABORTED");
});
