{
  "name": "@vlashex/shared-store",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean:src": "rimraf --glob \"src/**/*.js\" \"src/**/*.d.ts\" \"src/**/*.d.ts.map\" \"src/**/*.js.map\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@vlashex/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  }
}
